{
    "name": "volunteer-job-tracking",
    "version": "1.0.0",
    "description": "Volunteer Job Tracking System",
    "scripts": {
      "clean": "rm -rf dist",
      "build": "npm run clean && node build/generate.js",
      "dev": "npm run build && serve dist"
    },
    "dependencies": {
      "csv-parse": "^5.5.0"
    },
    "devDependencies": {
      "serve": "^14.2.1"
    }
  }
  